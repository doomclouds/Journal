# Journal 个人声明

Journal 是一个本地优先的晨间日记应用，面向长期个人记忆。

Journal 的基本立场如下：

- 保留用户的原始表达。
- 将正式日记保存为可读、可迁移的 Markdown 文件。
- 在写入正式条目前保留明确的用户确认边界。
- 将本地数据所有权和可访问性置于用户侧。

Journal 不以不透明自动化替代人的表达、判断和确认。AI 可以辅助整理结构，但用户的真实表达和最终确认始终是正式日记的中心。

一篇多年后重新打开的日记，应当仍是一份朴素、可读、可由普通工具打开的个人记录，而不是只能依赖特定黑箱服务解释的数据碎片。
